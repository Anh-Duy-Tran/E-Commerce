import styled from 'styled-components';

const Container = styled.div`
  display: grid;
  grid-template-rows: 50px 200px;
`

const CartItem = ({}) => {
  return (
    <Container>
      
    </Container>
  )
} 